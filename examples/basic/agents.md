# Open UDP — Basic examples

## Purpose
Minimal UDP flow: BROADCAST_ORDER → SUBMIT_BID → AWARD_ORDER → UPDATE_STATUS → DELIVERED.

## Retrieval
Use parent `examples/agents.md` and `open-udp/manifest/messages.md` for message schemas and flow.
